version: '3'
services:
  app:
    image: php:8.3-cli
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: php artisan serve --host=0.0.0.0 --port=8000
    init: true
